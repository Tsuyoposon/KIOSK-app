<%= form_for(@c_like) do |f| %>
  <% if @c_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c_like.errors.count, "error") %> prohibited this c_like from being saved:</h2>

      <ul>
      <% @c_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.number_field :comment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
